jQuery(function() {

    // Selectbox
    jQuery('select').selectpicker();

    // Checkbox and radios
    //jQuery('input:checkbox').prettyCheckable();
    //drujQuery('input:radio').prettyCheckable();
});